# SyncMaster UI

Static, self-contained web UI for SyncMaster. No build step and no dependencies: open `index.html` directly in any modern browser to see the working app with mock data. It is served as-is by the panel and embedded in the desktop shell's WebView.

Plain HTML + CSS (custom properties) + vanilla JS modules. System fonts only; no network requests.
